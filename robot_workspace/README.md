# VT Franka Workspace

This package is meant for the workstation side.

It owns:

- Quest teleoperation
- Quest feedback publishing
- GelSight capture and tactile visualization
- raw stream recording and offline alignment
- real-world rollout helpers

It talks to the controller machine through the controller HTTP API.

